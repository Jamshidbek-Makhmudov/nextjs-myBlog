/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,

  images: {
    domains: ["media.graphassets.com"],
    //buni biz qoshimcha yuklashimiz kerak, bomasa imagelarni oqimidi
  },
}

module.exports = nextConfig
